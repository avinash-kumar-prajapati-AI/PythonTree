# dependencies & build output
node_modules/
.vinxi/
.output/
.vercel/
dist/
app.config.timestamp_*.js

# local environment & secrets (the template .env.example stays committed)
.env*
!.env.example
*.local

# retired local SQLite database — kept on disk, never in the repo
data/

# local docs not needed for the app to run
DEPLOY.md

/.claude
